# Harness checklists

## PR / session checklist

- [ ] Touched contracts? Types exported and fixture still parses
- [ ] New fact sentences? Each has source ref
- [ ] Web-only secret leak? Search for `SLC_API_KEY` under `apps/web` → must be zero
- [ ] Demo path: brief → map → walk → source still works
- [ ] Colors still within Haipai 6

## Skeleton envelope minimum

- theme, logic_line, curator_note
- route.stops ≥ 5 with order, geo, minutes
- at least one stop with layers including `event` when claiming 多重人生
- blocks include ≥1 story_card
