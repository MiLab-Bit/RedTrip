# 红鸢 RedTrip · 微信小程序交付清单

> 仓库：`apps/miniprogram`  
> 同步目标：Cursor Origin（dev）`cat-dev/tmp-634d5d7771341c52` + GitHub `MiLab-Bit/RedTrip`  
> 更新日期：2026-08-26

---

## 1. 基本信息

| 项 | 内容 | 状态 |
|----|------|------|
| AppID | `wxc7953007477c1980` | ✅ |
| 项目名 | RedTrip / 红鸢 | ✅ |
| 代码目录 | `apps/miniprogram/` | ✅ |
| 业务 API | `https://sy-realm.ltd/redtrip` | ✅ |
| request 合法域名 | `https://sy-realm.ltd` | ✅ 已配置 |
| 最新上传版本 | `0.1.1`（CI 上传成功） | ✅ |
| 体验版 / 提审 | 需在公众平台点选 | ⏳ 人工 |

---

## 2. 功能范围（MVP）

| 页面 | 路径 | 能力 | 状态 |
|------|------|------|------|
| 出题 | `pages/brief` | 城市、起点联想、时长/调性/同伴/时段/对象；演示武康/外滩 | ✅ |
| 装订中 | `pages/loading` | `curate/start` + `curate/status` 轮询；L1/L2/L3 印章进度 | ✅ |
| 序章 | `pages/intro` | 主题、人物、章节脉络、降级提示 | ✅ |
| 章节阅读 | `pages/reader` | 站站正文、行前提示、上下站导航 | ✅ |
| 失败 | `pages/fail` | 友好失败 + 回出题 | ✅ |
| 隐私政策 | `pages/privacy` | 站内隐私文案 | ✅ |

**明确不做（本版）**：登录/BYOK、2.5D 地图、PDF/EPUB 导出、定位打卡。

---

## 3. 后端配套（小程序依赖）

| 接口 | 用途 | 状态 |
|------|------|------|
| `GET /v1/cities` | 城市列表 | ✅ |
| `GET /v1/places/suggest` | 起点联想 | ✅ |
| `POST /v1/curate/start` | 提交策展任务 | ✅ |
| `GET /v1/curate/status/{task_id}` | **轮询进度**（无 EventSource） | ✅ 已部署 |
| `GET /v1/demo/wukang` | 演示武康 | ✅ |
| `GET /v1/demo/yida` | 演示一大·外滩 | ✅ |
| `GET /v1/health` | 健康检查 | ✅ |

### 默认大模型（全访客，无需登录）

| 项 | 值 | 状态 |
|----|-----|------|
| Base | `https://chatapi.weixin.qq.com/openai/v1` | ✅ 已写入服务器 `.env` |
| Model | `GLM-5.2` | ✅ |
| Timeout | `900s` | ✅ |
| 思考关闭 | `chat_template_kwargs.enable_thinking=false` | ✅ curator + probe |

> Token 仅存服务器环境变量 / 用户 BYOK 库，**不入库到 git**。

### 网页账号 BYOK（`abovetigers@gmail.com`）

原 abc-ai.cn 两条配置已删除，替换为大赛 Token（text + multimodal 槽）✅。

---

## 4. 源码与资产清单

```
apps/miniprogram/
  app.js / app.json / app.wxss
  project.config.json          # AppID
  project.private.config.json  # urlCheck
  sitemap.json
  README.md                    # 开发说明
  SUBMIT.md                    # 上线操作（粘贴文案）
  .gitignore                   # private.key / node_modules
  scripts/upload.js            # miniprogram-ci 上传
  utils/
    config.js                  # API_BASE
    api.js                     # 请求 / 策展 / 演示
    defaults.js                # 出题默认值
    story.js                   # envelope → 阅读视图
    types.js
  pages/
    brief|loading|intro|reader|fail|privacy/
  assets/
    kite.png
    icon.png / icon-108|144|256|1024.png
```

相关文档：

| 文档 | 说明 |
|------|------|
| `apps/miniprogram/README.md` | 本地打开与差异说明 |
| `apps/miniprogram/SUBMIT.md` | 公众平台粘贴文案 + 提审步骤 |
| `docs/小程序交付清单.md` | **本清单（总览）** |
| 根 `README.md` | 已含小程序入口说明 |

---

## 5. 已完成事项（勾选）

- [x] 原生小程序脚手架 + AppID 写入
- [x] 出题页（含起点输入高度修复 0.1.1）
- [x] 策展轮询 API `GET /v1/curate/status/{id}`
- [x] 演示线秒开
- [x] 隐私页 + `__usePrivacyCheck__`
- [x] 头像多尺寸素材
- [x] request 合法域名配置（用户侧）
- [x] 代码上传密钥 CI 上传 `0.1.0` / `0.1.1`
- [x] 服务端大赛 LLM（GLM-5.2）默认配置
- [x] 网页账号两条旧 Token 替换为大赛 Token
- [x] 源码推送 Origin + GitHub `main`

---

## 6. 待你完成（公众平台）

- [ ] 管理 → 版本管理 → 将 **0.1.1** 设为**体验版**
- [ ] 真机扫码：演示武康 + 一次真策展
- [ ] 完善：名称 / 简介 / 类目 / 头像 / 隐私指引（文案见 `SUBMIT.md`）
- [ ] **提交审核**（审核说明见 `SUBMIT.md`）
- [ ] （可选）加体验成员微信号

---

## 7. 本地开发 / 再上传

```bash
# 微信开发者工具导入
apps/miniprogram

# CI 再传（需 private.key，勿提交 git）
cd apps/miniprogram
npm i miniprogram-ci --no-save
# 私钥 → private.key
WECHAT_APPID=wxc7953007477c1980 WECHAT_VERSION=0.1.2 \
  WECHAT_DESC='说明' node scripts/upload.js
```

**注意**：云端出口 IP 会变；代传时请保持「代码上传 IP 白名单」关闭，或改用本机开发者工具上传。

---

## 8. 验收烟测

```bash
BASE=https://sy-realm.ltd/redtrip
curl -sS "$BASE/v1/health" | head -c 200
curl -sS "$BASE/v1/cities" | head -c 200
curl -sS "$BASE/v1/demo/wukang" -o /dev/null -w "%{http_code}\n"
# 策展轮询
TASK=$(curl -sS -X POST "$BASE/v1/curate/start" -H 'Content-Type: application/json' \
  -d '{"slots":{"city":"shanghai","scene":"外滩","duration_min":90,"tone":"轻社交","companions":"2人","daypart":"day","audience":"成人","delivery":"路线"}}' \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["task_id"])')
curl -sS "$BASE/v1/curate/status/$TASK" | python3 -m json.tool | head -20
```

或：`BASE=https://sy-realm.ltd/redtrip bash scripts/smoke_sy_realm.sh`

---

## 9. 风险与安全

| 项 | 说明 |
|----|------|
| 大赛 Token / 上传私钥 / GitHub PAT | **禁止写入 git**；泄露需轮换 |
| `private.key` | 已在 `.gitignore`；用完删除本地副本 |
| GLM-5.2 thinking | 已在 curator 关闭，避免 content 为空 |

---

**结论**：小程序代码与后端配套已在 **dev（Origin）+ GitHub `main`**；剩余是公众平台体验版与提审点击。
