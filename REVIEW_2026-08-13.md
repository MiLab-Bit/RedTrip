# RedTrip 项目审查报告（架构 / 代码质量 / RT / Bug / 功能完整性）

> 审查日期：2026-08-13
> 范围：`packages/curator`（策展管线）、`packages/gate`（安全闸门）、`packages/library-client`（SLC 客户端）、`apps/api`（FastAPI）、`apps/web`（前端导出与叙事）
> 已修复：前端 `exportBook.ts` 的 `StoryView` 错误导入（见 Bug #1）。**未提交**（按"先不提交"约定）。

---

## 一、架构总览（结论：结构清晰、分层合理）

Monorepo 分层正确，依赖方向干净：

```
library-client  →  （SLC / OSM 等外部数据）
       ↓
   curator       →  （意图解析 → 取证 → 图层融合 → 红鸢词库 → 路线规划 → 叙事 → 润色+溯源 → 中间产物 → 闸门）
       ↓                                        ↑
   gate          →  （evaluate_envelope：发布前硬约束/告警）  红鸢 L2 词库 + L3 热词
       ↓
   contracts     →  （前后端共享 TS/Zod 契约）
       ↓
   apps/api  +  apps/web
```

管线关键路径（`pipeline.py::curate`）：
`parse_intent → fetch_evidence → join_layers → draw_voice_pack → plan_route → attach_layer3 → narrate → [build_artifacts ∥ _finalize_narrative] → embed → evaluate_envelope`

**亮点**
- LLM 调用已合并到 **2 次**（结构化命题分解 + 创意润色），且二者在 `ThreadPoolExecutor(max_workers=2)` 中并发，削平了串行等待。
- 句子级溯源已合并进润色同一次调用（B3），不再有独立的"溯源大调用"。
- 混合路由（cloud / hybrid / local）、预算熔断、回退模板等多重稳定性设计到位。

---

## 二、响应时间（RT）与"是否该用其他语言重写"——核心结论

**结论：把本地模块用 Rust / Go 重写，对 RT 几乎零收益。瓶颈不在本地计算，而在外部 LLM 网关。**

### RT 拆解（实测 ~160s 一次完整策展）
| 阶段 | 性质 | 耗时量级 |
|---|---|---|
| `fetch_evidence`（SLC 并发抓取 + 诗词 + 路段） | 网络 I/O，并发 ~12 workers | ~6–12s |
| `plan_route` / `join_layers` / `narrate` / `storycraft` / `build_artifacts`（JSON 拼装） | 纯本地 CPU | **< 1s** |
| 结构化 LLM（命题分解，role=structured） | 网络 I/O（网关） | 数十秒 |
| 创意 LLM（润色+溯源，role=creative，timeout=240s） | 网络 I/O（网关） | 数十秒～160s |

关键路径 = `max(结构化 LLM, 创意 LLM)` ≈ 一次网关往返。**所有本地代码加起来不到 1 秒**，即便把它优化到 0 也省不出什么。

### 哪些模块"看起来重"其实不重
- OSM footprint / 小红书热词 / SLC 抓取：都是网络 I/O，瓶颈在网络与第三方限流，不是语言。
- 叙事拼装 / 路线规划 / 图层融合：数据量上限 12 个点，纯 Python 字典/字符串操作，CPU 占用可忽略。

### 真正能降 RT 的杠杆（按性价比）
1. **把生产流量切到异步 SSE 路径**（`/v1/curate/start` + `/v1/curate/stream`）——同步 `/v1/curate` 会阻塞工作线程，遇到网关 90s+ 延迟极易触发网关超时回退（历史已踩过）。异步路径已写好但未设为默认。
2. **让异步路径也命中指纹缓存**（当前它完全不查缓存，见 Bug #4）。
3. **hybrid 策略下结构化调用走本地小模型**（ollama），省掉一次云端网关往返。
4. **进一步压缩 prompt 体积**（fact_catalog 已去重；仍可精简 route 卡/无用层）。
5. 若网关支持，**改用流式输出 + 更快模型**，并考虑对"相似场景"做语义缓存而非精确指纹。

> 一句话给决策层：**不要在语言重写上投入。** 唯一值得的"非 Python"投入是可选的本地推理进程（ollama）来卸载结构化调用——而这已经是 hybrid 策略支持的，只是部署没开。

---

## 三、Bug（按严重度排序）

### 🔴 Bug #1 — 前端编译错误（已现场修复）
`apps/web/src/features/story/exportBook.ts` 第 1–6 行从 `@redtrip/contracts` 导入 `StoryView`，但 `StoryView` **只在本地 `features/story/storyView.ts` 定义**，`@redtrip/contracts` 并不导出它（全仓 grep 确认）。

- 后果：`tsc` / `vite build` 直接失败，`exportBook.ts` 整文件无法编译 → PDF/EPUB 导出功能根本打不出来。
- 修复：改为 `import type { StoryView } from "./storyView";`（其余组件 StoryIntro / StoryOutro / WalkStage / NarrativeMap 都是这样写的，只有这一处写错）。
- 状态：已修复（未提交）。

### 🔴 Bug #2 — Gate 的 G4 硬约束实际失效（溯源安全网形同虚设）
`gate/engine.py` 的 G4 硬约束要求 `provenance.coverage_ratio == 1.0`（每条事实断言都要对齐 fact_uri），否则拦截发布。

但 `artifacts._provenance` 用 `ok = bool(rid)` 判定对齐，而 `rid = layer.source.record_id`。在 `evidence.py::_attach_corpus_layers` 中，语料图层的 record_id 是 `key or name or "?"`——**永远非空**，缺失时回退成字面量 `"?"`。

- 后果：coverage 永远是 1.0，G4 永远通过。一个 `record_id="?"` 的"假溯源"会被判定为"已溯源"。**"每个事实必须可溯源"这条产品核心承诺，当前无法被真正校验。**
- 建议：把 `record_id in (None, "", "?")` 视为未对齐（blocker）；或让 G4 在校验时剔除 `"?"` 占位。
- 状态：仅建议，未改（涉及安全语义，需你确认）。

### 🟡 Bug #3 — 死代码：`build_sentence_provenance` / `_collect_narrative`
`sentence_provenance.py` 里的 `build_sentence_provenance()` 与 `_collect_narrative()` **全仓无调用**（grep 确认）。句子级溯源已改为在 `polish._build_sp_report_from_polish` 中基于润色同一次 LLM 的 `story_provenance` 构建。这两段（含 B1/B2 注释描述的旧"独立大调用"方案）是过期实现。

- 后果：维护者会被误导以为存在独立的句子溯源通道；预算熔断逻辑也永远不会执行。
- 建议：删除或补一个单测。

### 🟡 Bug #4 — 异步路径不查缓存 + 无请求去重（惊群）
同步 `/v1/curate` 用 `_cache_get/_cache_put`（指纹缓存），但 **异步 `/v1/curate/start` 直接 `run_curator` 不查缓存**。同时两者都无请求级去重：多个相同请求并发进来，会同时打满 SLC + LLM 网关（网关并发仅 ~2 路），极易触发限流/超时回退。

### 🟡 Bug #5 — `_curate_tasks` 内存泄漏
`apps/api/main.py` 的 `_curate_tasks` 字典只增不删（无 TTL / 上限）。每次 `/v1/curate/start` 都把完整响应 dict 存进去，长期运行会持续增长。

### 🟡 Bug #6 — 同步路径吞掉异常静默回退
`/v1/curate` 的 `try/except` 把任意 `curator` 异常都包成 snapshot fixture 返回（status 仍是 ok）。生产环境真出问题会被"成功外壳"掩盖，难以观测。

### 🟢 Bug #7 — EPUB `dc:identifier` 非法 UUID
`exportBook.ts` 用 `urn:uuid:${safeTitle}-${Date.now()}` 作为 `dc:identifier`，这不是合法 UUID（epubcheck 会报错）。且 `Date.now()` 使其每次导出都变，破坏可复现/去重。

### 🟢 Bug #8 — `assert wp.buri` 用作运行时检查
`evidence.fetch_evidence` 用 `assert wp.buri` 做控制流。若以 `python -O` 运行，assert 被剥离，这里会直接抛 `AttributeError`。应改成显式 `if not wp.buri: continue`。

### 🟢 Bug #9 — 过时注释
`pipeline.py` 注释仍写"4 次串行 LLM ≈ 60s → 并发 ≈ 30s"，实际已是 2 次调用。会误导后续维护者。

---

## 四、功能不完整 / 待补

1. **异步 SSE 是"建好但未启用"的特性**：若前端仍调同步 `/v1/curate`，那套实时进度的 SSE 等于死功能。需要确认并把生产路径切过去（同时接缓存，见 Bug #4）。
2. **缓存版本与代码/语料/白名单脱钩**：缓存键只含 `CACHE_SCHEMA="v1"`。部署新代码/新语料后必须手动 bump 该常量或删除 `.curate_cache.json`，否则旧产物会按 TTL（默认 24h）持续返回。建议改成按代码版本/语料哈希自动失效。
3. **测试几乎为零**：后端 `curator`/`gate` 无单测/集成测试；前端仅有 `contract.test.ts`（契约形状）。管线、Gate、RAG 命中都靠手动验证，回归风险高。
4. **句子级溯源降级静默**：若模型未返回 `story_provenance`，整份 `sentence_provenance` 会是 `None`（Gate 仅 warn），没有补偿来源。
5. **`road_para` 文案拼接略生硬**：`storycraft` 里 `f"先认路，再认楼：{ev.road_context}楼是…"`，`{road_context}` 后直接接"楼是"，当 road_context 以句号或长句结尾时阅读不顺（不影响功能）。

---

## 五、代码质量（总体良好，细节待打磨）

**好的方面**
- 模块边界清晰，dataclass + 纯函数组合，副作用收口干净。
- 处处 fail-safe：LLM 不可用→回退模板；SLC 单点失败→跳过该维度；Gate 不过→回退证据叙事而非空白。
- LLM 客户端零三方依赖（仅 stdlib + urllib），部署轻。
- 混合路由 + 预算熔断 + 并发拼装，工程素养在线。

**待改进**
- `assert` 误用（Bug #8）、死代码（Bug #3）、过时注释（Bug #9）。
- `_fetch_batch` 用 `pool.map` 后 `break >= limit`，但 map 已把全部 worker 跑完（浪费，非错误）。
- 契约层 `RouteEnvelopeSchema` 字段极多，部分字段（如 `provenance`/`curated_story`）是"可选注入"，前后端对字段非空性的假设需要靠测试而非注释兜底。

---

## 六、优化优先级清单

### P0（必须现在做）
- [x] **Bug #1**：修复 `exportBook.ts` 的 `StoryView` 导入（已做）。
- [ ] **Bug #2**：让 Gate G4 真正校验溯源（剔除 `"?"`/空 record_id 为未对齐 blocker）。
- [ ] 生产流量切到异步 SSE 路径，并让异步路径命中缓存 + 加请求去重（Bug #4）。

### P1（近期）
- [ ] 删除死代码 `build_sentence_provenance` / `_collect_narrative`（Bug #3）。
- [ ] 缓存按代码/语料版本自动失效，替代手动 bump `CACHE_SCHEMA`。
- [ ] `assert wp.buri` → 显式 `if not wp.buri: continue`（Bug #8）。
- [ ] `_curate_tasks` 加 TTL/上限淘汰（Bug #5）。
- [ ] 非 demo 模式下暴露管线异常，而非静默 snapshot 回退（Bug #6）。

### P2（打磨）
- [ ] EPUB `dc:identifier` 改用 `crypto.randomUUID()` 并稳定（Bug #7）。
- [ ] 清理过时架构注释（Bug #9）。
- [ ] 补充 `curator`/`gate` 单测 + 一次端到端 `curate` 集成测试。
- [ ] 压缩 prompt、评估本地小模型卸载结构化调用以降 RT。

---

## 七、关于"用其他语言重写"的最终建议

**不建议。** 本系统的 RT 瓶颈是外部 LLM 网关（网络 I/O，占 ~99% 时间），本地 Python 计算 < 1s。把 Python 换成 Rust/Go 只能优化那 < 1s，用户体感不变，却要承担整库重写、招聘/维护成本、丢失 Python 在数据/AI 生态的便利。**唯一值得的"非 Python"投入是本地推理进程（ollama）来卸载结构化 LLM 调用——而 hybrid 策略已经支持，只需部署开启即可。**

---

## 八、第二轮修复执行记录（2026-08-13，按"全部修了"）

> 全部改动均**未提交**（沿用"先不提交"约定）。前端零依赖、本地无 node_modules，未跑 `tsc`，仅靠人工核对 + Python 端编译/导入/单测验证。

### 已修复
| Bug | 文件 | 改动 |
|---|---|---|
| #2 G4 溯源失效 | `evidence.py` | 语料图层 `record_id` 占位从 `"?"` 改为 `None`（缺失即真缺失）；`artifacts._provenance` 与 `_layer_to_fact` 把 `"?"`/空/None 视为未对齐；`gate.engine` Q2（layer 与 story_card 出处）把 `"?"` 视为缺 `record_id` |
| #3 死代码 | `sentence_provenance.py` | 删除 `build_sentence_provenance` / `_collect_narrative` / `_parse_json_obj` 及仅其使用的 `json/os/time/concurrent.futures/chat_completion/llm_configured` 导入；保留 `polish` 仍复用的 `_fact_catalog`/`_split_sentences`/`_heuristic_align` 与数据结构 |
| #4 异步不缓存/无去重 | `apps/api/app/main.py` | `curate_start` 先查指纹缓存（命中直接返回已完成任务），再对相同指纹在途任务去重；`_run_curate_bg` 成功后写入缓存 |
| #5 内存泄漏 | `apps/api/app/main.py` | 新增 `_evict_tasks()`：淘汰 >1h 的 done/error 任务并硬限总数（=CACHE_MAX） |
| #6 异常静默吞掉 | `apps/api/app/main.py` | 同步 `/v1/curate` 的顶层 `except` 改为返回 `status="degraded"` 并暴露异常原因（不再伪装成功） |
| #7 EPUB 非法 UUID | `exportBook.ts` | `dc:identifier` 改用按书名派生的稳定合法 UUID（同一本书每次一致，epubcheck 友好） |
| #8 `assert` 误用 | `evidence.py` | `assert wp.buri` → `if not wp.buri: continue`（-O 模式不再抛 AttributeError） |
| #9 过时注释 | `pipeline.py` | 注释改为"2 次 LLM 调用（结构化命题 + 创意润色）并发" |
| 缓存手动失效 | `apps/api/app/main.py` | 新增 `_compute_code_version()`：对核心代码+语料文件内容哈希，指纹纳入 `CODE_VERSION`，部署变更自动失效旧缓存（替代手动 bump `CACHE_SCHEMA`） |

### 验证
- Python 6 文件 `py_compile` 全通过；`packages/` 内无 `build_sentence_provenance`/`_collect_narrative` 悬空引用。
- 全量导入 `redtrip_curator` / `redtrip_gate` 无错误；polish 复用符号可解析。
- Gate 三场景单测：① 全真实溯源→通过；② 含未对齐断言→G4 拦截；③ `record_id="?"`→Q2 拦截。全部断言通过。

### 仍未做（超出"修 bug"范围，按需）
- 后端单测/集成测试（建议在 CI 固化上面的 Gate 三场景）。
- 句子级溯源降级补偿（模型未返回 `story_provenance` 时的备用来源）。
- `road_para` 文案拼接顺滑度微调（不影响功能）。
- 前端 `tsc`/`vite build` 仍需在本机 `npm install` 后实跑确认（改动仅为类型导入修正与 EPUB id，风险低）。

---

## 九、LLM 调用优化（回答"如何优化"）

管线关键路径已是 **2 次 LLM 调用并发**（结构化命题 + 创意润色），本地计算 < 1s。可继续压 RT 的杠杆（按性价比）：

1. **生产切到异步 SSE 路径**（本轮已接缓存+去重）：避免单次网关 >90s 触发同步路径的超时回退模板（历史踩过坑）。
2. **指纹缓存 + 请求去重**（本轮已做）：相同/相似查询 <2s 返回；去重避免惊群打满仅 ~2 路并发的网关。
3. **结构化调用走本地小模型**（ollama / hybrid 已支持，部署开启）：省掉一次云端网关往返，结构化任务对模型能力要求低，本地 7B 级足够。
4. **prompt 瘦身**：`fact_catalog` 已去重；可进一步裁掉未进正文的 route 卡 / 冗余层，给创意调用设 `max_tokens` 安全封顶（防小模型失控，不截断成品）。
5. **语义缓存**：当前是精确指纹；可对 query 做 embedding 相似度匹配，复用"近邻"已策展结果，命中率更高。
6. **更快/流式模型 + 低 temperature**：结构化调用用低延迟模型；若网关支持流式，润色边生成边渲染。
7. **预暖缓存**：部署时对热门场景（武康路、梧桐区）跑一次定时 curate，把成品预热进缓存。

**结论复述**：不要为 RT 换语言（Go/Rust）。收益上限是本地那 <1s，而瓶颈 99% 在外部 LLM 网关；真正降 RT 的只有"切异步 + 缓存去重 + 本地小模型卸载结构化调用 + prompt 瘦身"这几条，且全部无需改语言。

---

## 十、部署现状与分叉风险（🔴 最高优先级）

> 本轮"检查部署"的核心结论：**服务器跑的是前几轮修复之前的旧代码，且藏有本地没有的线上 auth 功能代码（半集成、编译不过、线上已断）。直接 scp 本地代码会把这套代码连同线上功能一起删掉。**

### 10.1 服务器代码陈旧（MD5 全漂移）
本地改动与部署版本对比（示例）：
- `packages/curator/redtrip_curator/artifacts.py`：本地 `037f742…` vs 部署 `ebe35e2…`
- `apps/api/app/main.py`：本地 `12dd027…` vs 部署 `b103841…`
- 前两轮所有修复（Gate G4、死代码、异步缓存、EPUB UUID、异常降级等）**均未上线**。
- 端口实为 **8799**（非 8000）；`/health` 返回 404，仅 `/v1/health` 存在；`web-dist` 构建于 04:49，早于最新前端改动。

### 10.2 服务器独有代码（本地缺失！）
文件清单对比确认，服务器独有：
- 前端：`features/auth/ModelConfigPanel.tsx`；`shared/lib/authClient.ts` 多 7 个导出（模型供应商 preset 等）
- 后端：`auth_router.py` / `auth_lib.py` / `auth_store.py` / `crypto.py`
- `main.py.bak-*` 中含 auth 挂载代码块（说明是某次部署覆盖 `main.py` 时丢掉的）

### 10.3 服务器源码树编译不过（已实测）
在服务器独立副本 `/tmp/rt-srv` 跑 `tsc -b --noEmit`，**唯一错误**：
```
src/app/App.tsx(154,12): error TS2741: Property 'onOpenModelConfig' is missing ...
```
即 `UserMenu` 要求 `onOpenModelConfig` 属性，但 `App.tsx` 只传了 `onOpenAuth`。说明这套 auth 功能**从未真正接通**。

### 10.4 线上 auth 功能已死（已实测）
- `GET /auth/v1/me` → 404
- `GET /v1/model-providers/presets` → 404
- 原因：运行中的 `main.py` **未挂载 `auth_router`**（其真实前缀是 `/v1`，不是 `/auth/v1`；本地前端仍停留在 auth-core 独立部署时代的旧基址设计）。

### 10.5 处置建议（部署前必做，按序）
1. **合并**：把服务器独有 auth 文件回流本地（防部署丢失线上代码）。
2. **修编译**：`UserMenu.onOpenModelConfig` 要么由 `App` 真正传入（开 ModelConfig 面板），要么改可选属性——让整树 `tsc` 通过。
3. **挂载**：`main.py` 挂载 `auth_router`（前缀 `/v1`），恢复线上 auth / 模型供应商预设能力。
4. **验证**：在服务器独立副本跑 `tsc` + `py_compile`，确认无悬空引用。
5. **再部署**：走固化脚本（见十一）。

> 全部改动沿用"先不提交 / 先不部署"约定，仅完成代码合并与验证，上线等你确认。

---

## 十一、网络优化

实测数据（来自部署服务器 `/opt/redtrip/web-dist`）：

| 资源 | 原文 | gzip 级别1 | gzip 级别6 |
|---|---|---|---|
| vendor js | 1,243,030 B | 408,883 B | 343,401 B |
| index css | 55,737 B | 15,156 B | 11,676 B |

- **gzip 级别**：当前未显式设置（`gzip_comp_level` 默认 1）。vendor js 从级别 1 提到 6 可再省 **~64KB**（约 16%）。nginx 默认 gzip 已包含 `application/json`，688KB 的 envelope JSON 同样受益。→ 建议显式 `gzip_comp_level 6`。
- **静态资源缓存**：实测 vendor js 响应**无任何 `Cache-Control` 头**。文件名带 hash（内容指纹），属不可变资源，应发 `Cache-Control: public, max-age=31536000, immutable`，让浏览器永久缓存、省去每次重验证。→ 缺失，待补。
- **`proxy_buffering` 全局关闭**：`/redtrip/v1/` 整段 `proxy_buffering off`，导致 688KB JSON 响应无法被 nginx 缓冲、传输更慢。该指令应**仅对 SSE 流式端点关闭**，普通 JSON 应开启缓冲。→ 待补。
- **首屏字体阻塞（国内致命）**：原 `index.html` 用阻塞式 `<link rel="stylesheet">` 引 Google Fonts（`fonts.googleapis.com` 在中国大陆不可达）→ 首屏白屏。→ **已本地修复**：改非阻塞加载 + 系统衬线兜底（`--font-serif-cn` 等），书卷气不丢。
- **three.js 打入首屏**：`@react-three/fiber` 等被打进 1.24MB 单一 vendor chunk，仅 `map` 状态使用 → 首屏浪费。→ **已本地修复**：`App.tsx` 对 `MapStage` 改 `lazy()` + `storyReader` 阶段预取；`vite.config.ts` 用 `manualChunks` 拆出 `three-scene` / `react-core` / `state-core` / `vendor`。

---

## 十二、内容排版

- **PDF 导出空白页（功能细节+排版）**：原 `exportPdf()` 用 `setTimeout(cleanup, 10000)`，若用户在打印对话框停留 >10s，内容会被抽走 → 空白页。→ **已修复**：改为 `afterprint` 事件 + `matchMedia('print').change`（Safari 补充）+ 60s 后每 5s 轮询三重信号，且 `cleanup()` 内 `if (mql?.matches) return` 确保打印中途绝不清理。
- **书籍级 print 样式**：`ensurePrintStyles()` 增补 `@media print`——章节段落 `text-indent:2em; text-align:justify`、封面 `break-after:page`、标题 `break-after:avoid-page`、`.srcs/.scene/.cast` `break-inside:avoid`、`orphans:2; widows:2`、`code` 断词，杜绝孤行/错页。
- **CSS 冲突**：`.chapter-hook` 被定义两次（扉页 1.02rem/italic 被裸 `.chapter-hook` 0.92rem 覆盖）。→ **已修复**：加 `.chapter-list-item .chapter-hook` 作用域限定，恢复扉页引言样式。

---

## 十三、功能细节

- **`curated_story.route` 浅引用双序列化**：`artifacts.py` 原 `curated_story["route"] = envelope.get("route")` 与 `envelope["route"]` 同一对象 → JSON 输出两遍 route（占 envelope 49%，约 21.7KB/44.6KB），且有未来 mutate 互污隐患。→ **已修复**：route 单一数据源留在 `envelope` 顶层；`curated_story` 不再携带；契约 `CuratedStorySchema.route` 改可选，前端回退读 `env.route`。
- **前端双次 zod 校验**：`toOutcome` 原对 688KB envelope 先 `CurateResponseSchema.parse` 再 `RouteEnvelopeSchema.parse`，校验两遍。→ **已修复**：仅解析外层响应，不再重复校验内部 envelope。
- **降级信号端到端**：见十五。

---

## 十四、美学

- **降级提示条书页化**：`CurateOutcome` 携带 `degraded/notices` 后，`App.tsx` 顶部渲染 `.degrade-banner`（赭石描边、微暖底，与书页同族），标注"这一程是降级结果：内容可读，但未走完整策展"，可收起——警示不刺眼，与整体书卷美学一致。`book.css` 配套样式（含 `-webkit-line-clamp:2` 限高）。
- **系统衬线兜底**：`index.html` 的 `--font-serif-cn` 链路（Noto Serif SC → Songti SC/STSong/SimSun → Georgia/serif）保证无网络字体时仍有书卷气质，不 fallback 到无衬线。

---

## 十五、逻辑严谨性

- **`_snapshot_response` 永远报 ok**：原 `status = "ok" if verdict.passed else "ok"` 两分支同值，快照兜底永远伪装成功，监控/调用方无法区分"真策展 / 合格兜底 / 不合格兜底"。→ **已修复**：`status = "ok" if verdict.passed else "degraded"`。
- **前端把 degraded 当失败（回归）**：改 `_snapshot_response` 后，前端 `curateRoute`/`curateRouteStream` 仍 `status!=="ok"→throw`，等于让后端所有兜底（快照样例/异常降级）失效变报错。→ **已修复**：统一 `CurateOutcome` 类型 + `toOutcome()`（仅当 `envelope` 缺失才 throw，`degraded = status!=="ok"`），快照/异常兜底在前端如实展示而非一刀切报错。
- **异常不再静默伪装**：同步路径顶层 `except` 已改返回 `status="degraded"` 并暴露原因（与 Bug #6 修复一致）。

---

## 十六、数据源策略（用户关切：高德 / OSM / 上图API(SLC) / 小红书）

> 背景：用户已采买高德 API，正重新评估小红书抓取与 OSM 的必要性，并指出 SLC 实为**上海图书馆 API**（文化/地方志/老照片数字资源）。

### 16.1 结论：三者不是替代关系，而是分层
| 源 | 给什么 | 能否替代 | 风险 |
|---|---|---|---|
| **高德** | POI 检索/地理编码/步行路径/实时 | 中国境内唯一靠谱"查询+路径"底座，不可替 | 商业授权、GCJ-02 |
| **上图 API（SLC）** | 文化权威内容（历史/典故/文献/老照片） | 与高德/小红书不重叠，是策展"文化正统性"来源 | 需确认坐标系（GCJ-02 或 BD-09） |
| **OSM** | 建筑轮廓+步行路网几何（免费 ODbL） | 路网可替商业路网 API；建筑轮廓**只能部分替**（中国覆盖不全），仅作底 | WGS-84，与高德叠加须 GCJ-02 校正 |
| **天地图（国家地理信息公共服务平台）** | **瓦片/地图服务**（矢量底图含建筑渲染要素、道路/水系要素、地名搜索、地理编码） | **不能作建筑轮廓 polygon 源**：官方不提供可下载的建筑轮廓矢量（网上"天地图建筑 SHP"是第三方用其底图**矢量化/水经注导出**，非官方、多标注"仅供学习"）。可作 three.js 的**离线底图纹理参考**，但非轮廓物料 | WGS-84（CGCS2000≈WGS84）；免费仅非商用，商用须申请授权；瓦片 QPS 限流（≤10） |
| **高德建筑层（AMap.Buildings）** | 高德 JS API 内的 3D 楼块显示层 | **不适用于本产品**：RedTrip 地图是自研 three.js 场景，非高德地图控件；该层是闭源显示层、无可下载几何，爬瓦片违反 ToS | 仅能在高德自家地图内开启 |
| **小红书** | 社会信号（当下在聊什么/出片/氛围） | **不重叠**于地图库；但抓取不全、脆、有 ToS 风险 | 反爬/登录墙、样本漂移 |

> 注：**上海市公共数据开放平台（data.sh.gov.cn）本身以 POI/气象/交通/物业等点状与统计数据集为主，不含建筑轮廓多边形**；**天地图官方也不提供建筑轮廓 polygon 下载**（属瓦片服务）。建筑轮廓真正"可程序化下载的免费源"只有 **OSM**（已在用）与 **Microsoft Building Footprints**（中国覆盖待验证）；**权威兜底**是 **上海市测绘院/规划资源局**（经申请或矢量服务获取，多为 WGS-84/CGCS2000，部分可能 GCJ-02）。需注意：网上流传的"天地图/上海建筑 SHP"多标注"仅供学习、不得商用"，**生产环境不能直接用**。

### 16.2 对两个追问的修正
1. **"OSM 能替换商业 footprint/路网 API 吗"——部分能，且前提要核实**：
   - 路网几何：能，免费方案里最成熟。
   - 建筑轮廓：不能全替，可考虑补 **Microsoft Building Footprints（免费 ODbL，与 OSM 同许可）** 填 OSM 缺口——但**必须先验证中国/上海覆盖**（已知 2021 版明确不含中国，2024 版号称"全球"但官方评估表未列东亚；若上海覆盖差则兜底不成立）。坐标同为 WGS-84，引入**不减少** GCJ-02 校正负担。
   - "省一笔钱"仅在你们**当前单独付费买 footprint/路网 API** 时成立；若本来就是高德顺带，则不成立。且引入 OSM 要计 GCJ-02↔WGS-84 校正成本。
2. **"小红书抓目前齐全吗"——不齐全，也不该指望齐全**：实时抓取样本受反爬限制、随时漂移，只能当抽样信号/种子，不能当 ground truth。让策展"齐全可靠"的 backbone 应是 **上图(权威)+高德(POI)**，小红书做"风味层"。建议降级为"离线快照+人工种子词"，不做线上硬依赖。

### 16.3 坐标系铁律
高德(GCJ-02) / 上图(需确认 GCJ-02 或 BD-09) / OSM(WGS-84) 三者混用**必须做偏移校正**（如 `coordtransform`），否则叠加错位数百米。这是引入 OSM 或上图前必须先解决的基础设施。

> 修正：引入 **MS BF / 上海测绘院**（均 WGS-84/CGCS2000）**不会减少** GCJ-02 校正负担——它们和高德(GCJ-02) 混用仍要偏移。干净做法是：**所有轮廓源保持 WGS-84，把高德来的 route/POI 反算成 WGS-84 再进 `project`**，一道变换全局对齐，别在轮廓层做 GCJ-02。

### 16.4 建筑轮廓接入点已确认（最小改动方案）
代码实查结论：轮廓物料在后端取、前端只消费 `FootprintFeature[]`，**接新源前端零改动**。

- **取数真位置**：`packages/library-client/redtrip_library/osm.py` 的 `fetch_building_footprints(south,west,north,east,limit)` → 返回 `FeatureCollection`（`source="OpenStreetMap/Overpass"`，WGS-84）。
- **分发真位置**：`apps/api/app/main.py:367 map_footprints()`。当前 `REDTRIP_OSM_PREFER_FIXTURE=1` 默认走 fixture（走廊缓存示意），`=0` 才真拉 Overpass；预算 `REDTRIP_OSM_BUDGET_S=14` 超时回 fixture。
- **前端消费**：`footprints.ts:fetchFootprints()` → `/v1/map/footprints` → `osm/pipeline.ts`（project→simplify→cull→extrude 烤成 `THREE` 楼块）。`FootprintFeature` 类型：`{type, properties?{osm_id,height_m,height_schematic,name,levels}, geometry:{type,coordinates:number[][][]}}`。

**接 MS BF / 测绘院的最小改动**：
1. 在 `redtrip_library/` 新增 `msbuildings.py`（或 `survey.py`），导出 `fetch_ms_building_footprints(...)` / `fetch_survey_footprints(...)`，返回**同结构** `FeatureCollection`（`source` 标注），WGS-84 直接可用。
2. `redtrip_library/__init__.py` 暴露新函数。
3. `main.py.map_footprints()` 分层：`fixture`（env 可选）→ 主源(OSM live) → 备选源(MS BF/测绘院 live) → fixture fallback；用 `REDTRIP_FOOTPRINT_SOURCES="osm,msbuildings"` 控制。
4. 前端 `FootprintFeature` 类型与 pipeline **不动**（只要新源返回同结构）。
5. 仅当新源坐标系非 WGS-84 时，在取数函数内完成→WGS-84 变换；高德 route 侧统一反算 WGS-84。

**前置条件（决定走哪条）**：
- MS BF：须先验证上海覆盖（2021 版明确不含中国；2024 版"全球"但官方评估未列东亚——可用 `dataset-links.csv`/Planetary Computer STAC 按 quadkey 查上海是否有瓦片）。
- 上海测绘院/规划资源局：最权威、最稳，但**申请制 + 商用授权**，周期长。

---

## 十七、本轮（第三轮）本地修复执行记录

> 全部改动均**未提交、未部署**（沿用"先不"约定）。前端零依赖、本地无 node_modules，`tsc` 未在本机实跑；类型正确性靠服务器独立副本 `/tmp/rt-verify` 人工核对 + 后续需在 `/tmp/rt-merge` 重验。

### 已写入的本地修复
| 维度 | 文件 | 改动 |
|---|---|---|
| 美学/首屏 | `apps/web/index.html` | Google Fonts 改非阻塞加载 + 系统衬线兜底变量（`--font-serif-cn` 等） |
| 网络/首屏 | `apps/web/src/app/App.tsx` | `MapStage` 改 `lazy()` + `storyReader` 预取；`CurateOutcome` 接线 + 降级条 UI |
| 网络 | `apps/web/vite.config.ts` | `base` 固化（读 `VITE_BASE`）+ `manualChunks` 拆 `three-scene/react-core/state-core/vendor` + `chunkSizeWarningLimit 1600` |
| 网络 | `apps/web/package.json` | 固化双部署目标：`build:pages`(`--base=/`) / `build:nginx`(`--base=/redtrip/`) |
| 功能/排版 | `apps/web/src/features/story/exportBook.ts` | 打印清理改三重信号；书籍级 print 样式；EPUB 稳定 UUID（第二轮） |
| 排版 | `apps/web/src/styles/book.css` | `.chapter-hook` 加 `.chapter-list-item` 作用域；降级条样式 |
| 功能 | `apps/web/src/shared/lib/curate.ts` | `CurateOutcome` 类型 + `toOutcome()`；单次 zod 校验；EventSource 清理重构 |
| 逻辑 | `apps/api/app/main.py` | `_snapshot_response` 改 `ok/degraded` 区分 |
| 功能 | `packages/curator/redtrip_curator/artifacts.py` | `curated_story` 不再重复携带 `route`（单一数据源 `envelope` 顶层） |
| 契约 | `packages/contracts/src/index.ts` | `CuratedStorySchema.route` 改可选 |

### 待办（未做，需你拍板）
- [ ] **部署分叉合并**：回流服务器独有 auth 文件 + 修 `UserMenu.onOpenModelConfig` 编译 + 挂载 `auth_router`（见十）。
- [ ] 前端 `tsc`/`vite build` 在服务器副本实跑确认（本轮类型改动较多，含 `CurateOutcome` 全链路）。
- [ ] nginx 加固：显式 `gzip_comp_level 6`；静态资源加 `Cache-Control: immutable`；`proxy_buffering` 仅 SSE 关闭。
- [ ] 数据源策略定稿：建筑轮廓**可程序化免费源实质仅 OSM（已在用）+ MS BF（须先验证上海覆盖，2021 版不含中国）**；权威兜底为**上海市测绘院/规划资源局**（申请制+商用授权）。**天地图/上海数据开放平台均不提供建筑轮廓 polygon 下载**，前者是瓦片服务、后者是 POI/统计数据集，二者都不作轮廓物料源（已更正此前误判）。接新源改动在 `redtrip_library/*` + `main.py.map_footprints`，前端零改动（见 16.4）。

---

## 十八、第四轮：auth 分叉合并 + 验证（P0/P1）+ 数据源定论（P2）+ 线上隐患实锤

> 本轮把服务器独有但本地缺失的 **auth 功能代码**完整回流本地、修复编译、挂载路由，并在服务器独立副本 `/tmp/rt-merge` 通过全量验证。仍**未提交、未部署**。

### 18.1 auth 分叉合并（P0）——已完成
服务器 `/opt/redtrip` 里有一套本地**完全没有**的登录/模型配置系统（用户自带大模型 Key），但处于「半集成、从未接通」状态：后端 `auth_router` 没挂载（线上 404），前端 `UserMenu` 缺 `onOpenModelConfig` 必填属性（tsc 报错）。直接 scp 本地代码会把这套代码连同线上功能一起删掉，因此先回流。

**合并内容（已写入本地仓库）：**
| 文件 | 动作 | 说明 |
|---|---|---|
| `apps/api/app/auth_router.py` | 新增 | 前缀 `/v1`：`/v1/auth/*`、`/v1/me`、`/v1/model-providers/*`、`/v1/apikeys/*` 共 19 路由 |
| `apps/api/app/auth_lib.py` | 新增 | 零依赖密码学：PBKDF2 + HS256 JWT |
| `apps/api/app/auth_store.py` | 新增 | SQLite 数据层（users/email_verifications/api_keys/model_providers），`REDTRIP_DB_PATH` 可配 |
| `apps/api/app/crypto.py` | 新增 | Fernet 密钥加密（用户 API Key 落库前加密） |
| `apps/web/src/features/auth/ModelConfigPanel.tsx` | 新增 | 模型配置模态框（文本/多模态双槽位） |
| `apps/web/src/shared/lib/authClient.ts` | 覆盖 | 旧版用 `AUTH_BASE`（`/auth/v1` 旧前缀）；新版用 `API_BASE`（`/v1`）+ 5 个 model-providers 函数 |
| `apps/web/src/shared/lib/authConfig.ts` | 覆盖 | `API_ROOT ?? "/redtrip"`，与 nginx `/redtrip/*` 对齐 |
| `apps/web/src/features/auth/UserMenu.tsx` | 覆盖 | 增加「模型配置」入口（`onOpenModelConfig`） |
| `apps/web/src/features/auth/AuthModal.tsx` | 覆盖 | 服务器新版文案（注册后需邮箱验证） |
| `apps/web/src/features/auth/auth.css` | 覆盖 | 服务器超集（新增 `.auth-float`、`.verify-banner`） |
| `apps/web/src/app/App.tsx` | 编辑 | 接线 `ModelConfigPanel` + `UserMenu onOpenModelConfig` |
| `apps/web/vite.config.ts` | 编辑 | dev 代理补 `/redtrip/v1`、`/redtrip/auth/v1`、`/auth/v1`（全部 → 8799，去掉 8787 旧 auth-core） |
| `apps/api/app/main.py` | 编辑 | 文件尾挂载 `auth_router`（try/except，与服务器备份版一致） |
| `apps/api/requirements.txt` | 编辑 | 补 `httpx>=0.27.0`、`cryptography>=42.0.0` |
| `apps/web/src/features/auth/authStore.ts` | 不变 | 两端完全一致 |

**合并中处理的两处隐患：**
1. **路由冲突**：`auth_router` 自带 `GET /v1/health`，与 `main.py` 已有的 `@app.get("/v1/health")` 重复（服务器从未挂载所以从未暴露）。本地版已删除该端点并注释原因。
2. **API 前缀统一**：旧本地 `authClient` 用 `/auth/v1`（auth-core 独立部署时代的残迹），已随 `authConfig` 一并统一到 `/redtrip/v1`（dev 由 vite rewrite、prod 由 nginx rewrite 剥前缀，两跳都验证过）。

### 18.2 验证结果（P1）——全部通过
在服务器独立副本 `/tmp/rt-merge`（从 `/opt/redtrip` 复制，绝不触碰生产目录）：
- **前端 `tsc -b --noEmit` → EXIT 0**（含 ModelConfigPanel/UserMenu/App 接线，最终代码含 apiBase 改动后复跑仍 0）。
- **后端 compileall（venv Python 3.11.13）→ 通过**。⚠️ 注意：服务器系统 `python3` 是 **3.6.8**（不支持 `from __future__ import annotations`），任何编译/启动必须用 `/opt/redtrip/.venv/bin/python`。
- **auth 路由注册：`AUTH_ROUTES=19`，完整 app `total_routes=31`，`DUP_PATHS=[]`，`AUTH_MOUNTED=True`**（`[auth] unified login router mounted` 打印确认）。
- **venv 依赖**：`cryptography 50.0.0`、`httpx 0.28.1` **均已安装** → 部署后 auth 可直接挂载，无需补装。

### 18.3 线上隐患实锤：已部署前端的策展链路是 404（P2 附带发现）
- 实测（abc-ai.cn 线上）：`GET /v1/curate` → **404**；`GET /redtrip/v1/health` → **200**。
- 原因：已部署构建（web-dist，04:49）的 JS 里 API 路径是根相对的 `/v1/curate`（`apiBase.API_BASE=""`），而 nginx 只路由 `/redtrip/v1/*` → 策展/足迹/热词请求全部落空。
- **修复（已写入本地）**：`apps/web/src/shared/lib/apiBase.ts` 默认值 `""` → `"/redtrip"`，与 `authConfig.API_ROOT` 统一；dev 由 vite 代理 `/redtrip/v1 → 8799`、prod 由 nginx `/redtrip/v1/` 剥前缀承接，零环境依赖。Pages 部署仍可用 `VITE_API_BASE` 覆盖。

### 18.4 数据源定论（P2）——MS BF 不含上海
- 实测 MS BF 官方 `buildings-coverage.geojson`：**无中国/上海/北京要素**（东亚仅日本片段），2021 版（7.77 亿）明确不含中国，2024「全球版」覆盖表亦未列东亚。
- **结论：建筑轮廓兜底走「OSM 主线（已在用，WGS-84）+ 上海市测绘院/规划资源局（申请制、权威、商用需授权）」**；MS BF 从候选剔除（上海无数据）。天地图/上海数据开放平台均非 polygon 源（前述已更正）。

### 18.5 nginx 加固补丁（P3）——仅文本，未应用
基于线上 `fasttoken.conf` 实测（vendor js 1.24MB→gzip 级1 为 399KB，级6 省 ~64KB；静态资源**零 Cache-Control**；`/redtrip/v1/` 全局 `proxy_buffering off` 拖慢 688KB JSON）：

```nginx
# 1) http 块（/etc/nginx/nginx.conf）——压缩等级 1→6
gzip_comp_level 6;

# 2) /redtrip/ 静态块——哈希资源长缓存，index.html 不缓存
location /redtrip/ {
    alias /opt/redtrip/web-dist/;
    index index.html;
    try_files $uri $uri/ /redtrip/index.html;
    location ~* ^/redtrip/assets/.*\.(js|css|woff2?|png|jpg|jpeg|svg|webp|ico)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }
    location = /redtrip/index.html {
        add_header Cache-Control "no-cache";
    }
}

# 3) /redtrip/v1/ 块：去掉全局 proxy_buffering off，仅 SSE 端点关闭
#    （注意嵌套 location 需自带 proxy_pass/rewrite，proxy_set_header 会继承）
location /redtrip/v1/ {
    # ... 原 CORS/超时配置不变，删除 proxy_buffering off; proxy_cache off;
    location = /redtrip/v1/curate/stream/{task_id} {
        rewrite ^/redtrip(/.*)$ $1 break;
        proxy_pass http://127.0.0.1:8799;
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_read_timeout 3600s;
    }
}
```

> 应用前提：先完成 auth 分叉合并部署（含 `apiBase` 默认值修复 → 前端需 `vite build --base=/redtrip/` 重建），否则新前端路径 `/redtrip/v1/*` 与旧 nginx 配置不匹配。

### 18.6 当前状态与待办（未提交、未部署）
- [x] P0 auth 合并（本地完成） | [x] P1 rt-merge 验证（tsc/compileall/路由注册全过）
- [x] P2 MS BF 上海覆盖验证（无上海数据，剔除）
- [x] P3 nginx 加固补丁（文本已备，未应用）
- [ ] **部署**（仍等你拍板）：① 把合并后代码 rsync 到 `/opt/redtrip`（保留 `.env`、`data/`、`.curate_cache.json`、`web-dist`）；② `cd apps/api && /opt/redtrip/.venv/bin/pip install -r requirements.txt`（httpx/cryptography 已装则跳过）；③ 前端 `cd apps/web && pnpm install && npm run build:nginx`；④ 应用 nginx 补丁 + `nginx -t && systemctl reload nginx`；⑤ `systemctl restart redtrip-api`；⑥ 冒烟：`/redtrip/v1/health`、`/redtrip/v1/model-providers/presets`、注册/登录流。
- [ ] **数据源**：建筑轮廓申请上海市测绘院（商用授权）；坐标统一（高德 GCJ-02 反算 WGS-84 进 `project`）。

---

## 十九、线上断链修复部署执行记录（2026-08-13 08:50–08:53，已上线）

> 用户授权"意外实锤：线上策展链路当前是断的 这个你修复"。本次为**首次实际部署**（此前均"先不部署"），全程备份可回滚，未 git commit（沿用约定）。

### 修复内容
| 项 | 说明 |
|---|---|
| 前端 API 基址 | `apiBase.API_BASE` 默认 `"" → "/redtrip"`，与 `authConfig.API_ROOT` 统一；dev 由 vite 代理、prod 由 nginx rewrite 承接 |
| 后端 auth 挂载 | `main.py` 尾部 `app.include_router(auth_router)`（try/except），auth 19 路由上线 |
| 前端重建 | `pnpm run build:nginx`（tsc + vite build --base=/redtrip/），首屏 1.24MB → ~385KB（gzip ~118KB），`three-scene` 939KB 独立懒加载 |
| nginx | `gzip_comp_level 6`；`/redtrip/assets/*` 哈希资源 `Cache-Control: immutable`（`root + rewrite` 而非 `alias`，避免目录 301 坑）；index.html `no-cache` |

### 部署步骤（已执行）
1. 前置检查：`.env` 已有 `REDTRIP_AUTH_SECRET/REDTRIP_EMAIL_BASE_URL/REDTRIP_AUTH_REQUIRE_VERIFIED`；`data/redtrip.db`(53KB)+`.crypto_key` 已存在 → auth 直接复用，不重建数据。
2. 备份：`/opt/redtrip/backups/20260813-0850/`（main.py 全系、web-dist、.env、data）+ `/etc/nginx/nginx.conf.bak-20260813`、`fasttoken.conf.bak-20260813`。
3. 同步：本地合并代码 tar → `/opt/redtrip` 解包（排除 .env/data/web-dist/.curate_cache.json/.git/node_modules）。
4. 构建：服务器 `/opt/redtrip/apps/web` 内 `npm run build:nginx` 成功（37s，648 模块；有 manualChunks 循环引用警告，产物可用）；产物 `cp -a apps/web/dist/* web-dist/`。
5. nginx：补丁应用 → `nginx -t` 过 → `systemctl reload nginx`。
6. 重启：`systemctl restart redtrip-api` → active（新进程 263273）。
7. 冒烟（全部通过）：
   - `GET /redtrip/v1/health` → 200（mode=indexed, slc_key_configured=true）
   - `GET /redtrip/v1/model-providers/presets` → 200（auth 已挂载；此前 404）
   - 静态 JS → **200** + `cache-control: max-age=31536000, immutable`；index.html → 200 + `no-cache`
   - 未登录 `GET /v1/model-providers` → 401（Bearer 保护生效）
   - 错误密码 `POST /v1/auth/login` → 401（无副作用）
   - **策展端到端**：`POST /redtrip/v1/curate/start` → task `80e4063d…`；SSE `GET /redtrip/v1/curate/stream/{id}` → 实时 `progress: 82% · 叙事初稿完成`（前端 EventSource 全链路通）

### 回滚点
- 代码/数据：`/opt/redtrip/backups/20260813-0850/`
- nginx：`/etc/nginx/nginx.conf.bak-20260813`、`/etc/nginx/conf.d/fasttoken.conf.bak-20260813`
- 回滚即：恢复备份 → `systemctl restart redtrip-api` + `systemctl reload nginx`

### 遗留（非阻塞）
- `manualChunks` 循环引用警告（vendor↔react-core↔state-core）：产物可用，后续可调 chunk 边界消除。
- `proxy_buffering off` 仍在 `/redtrip/v1/` 全局（688KB JSON 未受益于缓冲）——SSE 与大数据响应共处一块，需嵌套 location 分离后再优化，本轮不动以避免 SSE 回归。
- 策展 creative 阶段约 160s（外部 LLM 网关），非本轮修复范畴。
- 本轮部署未 git commit；服务器代码为部署副本，与本地工作区一致（除 .env/data/web-dist 等运行时物）。

---

## 二十、三个遗留项修复（2026-08-13 09:00–09:10，已上线）

### 20.1 manualChunks 循环引用警告 —— ✅ 已消除并部署
- 根因：`zustand` 依赖的 `use-sync-external-store` 落入 vendor（vendor→react-core 边），`@xstate/react` 可能被 `/react/` 规则误抓，形成 vendor↔react-core↔state-core 三角环。
- 修法（`vite.config.ts`）：`use-sync-external-store` 显式归入 `state-core`；`@xstate/*` 优先匹配（在 `/react/` 规则之前）归 `state-core`；`react-is` 归 `react-core`。匹配顺序 three-scene → state-core → react-core → vendor。
- 验证：`/tmp/rt-merge` 迭代构建 **零 Circular 警告**；生产重建（14.4s）同样零警告。产物：react-core 197KB / state-core 46KB / vendor 57KB / three-scene 940KB（懒加载），首屏体积不变。
- 副作用收益：chunk 归属确定，浏览器加载顺序不再依赖循环解析。

### 20.2 proxy_buffering 全局 off —— ✅ 已修复并部署
- 关键发现：`main.py` 的 SSE `StreamingResponse` 响应头自带 `X-Accel-Buffering: no` → nginx 见头即对该响应跳过缓冲，**即使 location 默认 buffering on**。
- 修法（nginx）：删除 `/redtrip/v1/` 块的 `proxy_buffering off;`（保留 `proxy_cache off`），688KB JSON 响应恢复缓冲（nginx 缓冲后按客户端速度发送、释放上游连接）；SSE 靠响应头兜底不缓冲。
- 验证：SSE 流 body 0.046s 即时返回 done 事件（未缓冲）；JSON health 200。**无嵌套 location 需求，零回归风险。**

### 20.3 creative ~160s —— 已确认根因，部分缓解（根治待决策）
- 根因（已查代码 + 配置）：`_finalize_narrative → polish_envelope()` 是**单次大输出 LLM 调用**（一次精修整本：theme/logic_line/why_visit/curator_note + 每 stop meaning/transition + 每 block story_cards）；`llm.py` 为纯 stdlib urllib **非流式**同步等待完整 JSON；生产模型已是快档 `Qwen-flash`（自家网关 abc-ai.cn/v1，超时 240s）；**无重试放大**。
- 结论：160s ≈ 大输出 × 网关，**无法在应用层消除到秒级**，除非：① polish 拆片并行（theme/逐 stop/逐 block 并发小调用 → 预计 160s→~60s，中风险重构，建议单独一轮做）；② 章节级流式呈现（架构改动大）；③ 输出量裁剪（产品取舍）。
- **已上线缓解**：前端 loading 在 `narrate` 阶段显示「叙事精修中（模型生成较慢，约 2-3 分钟）」——明确预期，避免误判卡死。缓存复用已在（同请求二次秒回）。

### 20.4 本轮部署冒烟（全过）
- 首页 200 + 引用新 hash chunk；新 JS 200 + `immutable` 缓存头；`/redtrip` 常量在新 index chunk（策展路径正确）；`/redtrip/v1/health` 200；narrate 提示文案已进产物。
- 回滚点不变：`/opt/redtrip/backups/20260813-0850/` + nginx `.bak-20260813`。


